# =============================================================================
# Gecko Agent Landing — Dépendances production
# Installer avec : pip install -r requirements.txt
# =============================================================================

# --- Phase 1 (actif) ---
Flask>=3.1.0,<4.0.0
python-dotenv>=1.0.0

# --- Phase 3 (waitlist) — actif ---
Flask-WTF>=1.2.1
Flask-SQLAlchemy>=3.1.1
Flask-Migrate>=4.0.5
Flask-Limiter>=3.5.0
email-validator>=2.1.0

# --- Phase 5 (déploiement prod) — actif ---
# Gunicorn ne fonctionne pas sur Windows (Unix-only).
# Le marker conditionnel l'exclut automatiquement de l'install Windows.
gunicorn>=23.0.0; sys_platform != "win32"
Flask-Talisman>=1.1.0
# psycopg2-binary requis si tu bascules sur PostgreSQL en prod (sinon SQLite suffit)
psycopg2-binary>=2.9.9; sys_platform != "win32"

# --- Déploiement statique Netlify (génération HTML pré-rendu) ---
# Frozen-Flask parcourt les routes Flask et écrit le HTML dans build/
Frozen-Flask>=1.0.2
